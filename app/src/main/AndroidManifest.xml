<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.meetingapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".SignupActivity"
            android:label="회 원 가 입" />
        <activity
            android:name=".ProfileRegisterActivity"
            android:label="프로필 등록" />
        <activity
            android:name=".IntroductionActivity"
            android:label="이성 소개" />
        <activity
            android:name=".FillterChoiceActivity"
            android:label="소개 조건 선택" />
        <activity
            android:name=".FillterIntroductionActivity"
            android:label="필터링 이성 소개" />
        <activity android:name=".HartListActivity" />
        <activity
            android:name=".SendHartListUserInfoActivity"
            android:label="이성 프로필" />
        <activity
            android:name=".GetHartListUserInfoActivity"
            android:label="이성 프로필" />
        <activity
            android:name=".MatchingListUserInfoActivity"
            android:label="매칭 이성 프로필" />
        <activity
            android:name=".MyPageActivity"
            android:label="마이페이지" />
        <activity
            android:name=".ProfileEditActivity"
            android:label="프로필 내용 수정" />
        <activity
            android:name=".PhotoEditActivity"
            android:label="프로필 사진 수정" />
    </application>

</manifest>